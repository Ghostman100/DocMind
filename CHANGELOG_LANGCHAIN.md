# LangChain Integration - Changelog

## Дата: 2025-11-06

### 🎉 Добавлен полный функционал LangChain интеграции

Реализована двухпутевая архитектура: оригинальная система продолжает работать, новые LangChain endpoints добавлены параллельно.

---

## 📦 Новые зависимости

Добавлены в `requirements.txt`:
- `langchain==0.3.7` - Core LangChain framework
- `langchain-core==0.3.15` - Core components
- `langchain-qdrant==0.2.0` - Qdrant vector store integration
- `langchain-community==0.3.5` - Community loaders
- `langchain-text-splitters==0.3.2` - Text splitting utilities
- `langchain-openai==0.2.5` - OpenAI LLM integration

---

## 🏗️ Новая структура модулей

```
app/
└── langchain_integration/          # НОВОЕ
    ├── __init__.py                 # Экспорты модуля
    ├── schemas.py                  # Pydantic схемы для LangChain endpoints
    ├── vector_store.py             # QdrantVectorStore wrapper
    └── summarizer.py               # Суммаризация и извлечение пунктов
```

---

## 🔌 Новые API Endpoints

### 1. POST /langchain/ingest
- **Описание**: Альтернативная загрузка документа через LangChain RecursiveCharacterTextSplitter
- **Требования**: Нет (работает без LLM API ключа)
- **Совместимость**: Документы можно искать через `/query` или `/langchain/query`

### 2. POST /langchain/query
- **Описание**: Семантический поиск через LangChain retriever
- **Требования**: Нет (работает без LLM API ключа)
- **Совместимость**: Работает с документами из `/ingest` или `/langchain/ingest`

### 3. POST /langchain/summarize ⭐ NEW FEATURE
- **Описание**: Создание сокращенной версии документа через LLM суммаризацию
- **Требования**: `LANGCHAIN_ENABLED=true`, `LLM_API_KEY` в .env
- **Стратегии**: stuff / map_reduce / refine
- **Use case**: Получить краткое резюме длинного тендерного документа

### 4. POST /langchain/extract_points ⭐ NEW FEATURE
- **Описание**: Извлечение конкретных пунктов из документа по темам
- **Требования**: `LLM_API_KEY` только если `summarize=true`
- **Use case**: Найти и суммаризовать разделы "Требования", "Сроки", "Критерии оценки"

---

## ⚙️ Изменения в конфигурации

### Обновлен `app/config.py`

Добавлены новые настройки:
```python
# LangChain интеграция
langchain_enabled: bool = False
llm_provider: Literal["openai", "anthropic"] = "openai"
llm_api_key: str | None = None
llm_model: str = "gpt-4o-mini"
llm_temperature: float = 0.0
```

### Обновлен `.env.example`

Добавлены:
```env
# LangChain integration settings
LANGCHAIN_ENABLED=false
LLM_PROVIDER=openai
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.0
```

---

## 📚 Новая документация

### 1. LANGCHAIN_SETUP.md
- Quick start guide для LangChain функционала
- Примеры использования всех новых endpoints
- Информация о стоимости API
- Troubleshooting

### 2. Обновлен CLAUDE.md
- Добавлен раздел "LangChain Integration"
- Документация архитектуры двухпутевой системы
- Описание новых endpoints
- Конфигурация и зависимости

---

## 🔄 Совместимость

### Backward Compatibility
✅ **Все существующие endpoints работают без изменений**:
- POST /ingest
- POST /query
- POST /embed
- POST /test
- GET /health

### Cross-System Compatibility
✅ **Документы совместимы между системами**:
- Документ загруженный через `/ingest` можно суммаризовать через `/langchain/summarize`
- Документ загруженный через `/langchain/ingest` можно искать через `/query`
- Оба используют одну коллекцию Qdrant ("documents2")
- Метаданные совместимы (document_id, document_name, chunk_index, etc.)

---

## 🎯 Use Cases

### Use Case 1: Быстрая суммаризация тендерного документа
```python
# 1. Загрузить документ
POST /langchain/ingest
# 2. Получить краткое резюме
POST /langchain/summarize {"document_id": "...", "strategy": "map_reduce"}
```

### Use Case 2: Извлечение конкретных пунктов
```python
POST /langchain/extract_points {
    "document_id": "...",
    "topics": ["Требования", "Сроки", "Критерии"],
    "summarize": true
}
```

### Use Case 3: Сравнение моделей embedding (существующий функционал)
```python
# Загрузить с моделью 1
POST /ingest
# Создать embedding с моделью 2 для сравнения
POST /embed {"model": "intfloat/multilingual-e5-base", "text": "..."}
```

---

## 🔒 Security & Cost Considerations

### API Key Security
- LLM_API_KEY хранится в `.env` (не в git)
- Эндпоинты проверяют наличие ключа перед выполнением
- Базовые эндпоинты (`/langchain/ingest`, `/langchain/query`) работают без ключа

### Cost Management
- По умолчанию `LANGCHAIN_ENABLED=false` (требует явного включения)
- Использование `gpt-4o-mini` для экономии
- Параметр `max_chunks` ограничивает обработку
- Логирование всех LLM вызовов

---

## 🧪 Testing

### Рекомендуемый тест-план:

1. **Базовая загрузка без LLM**:
   ```bash
   curl -X POST http://localhost:8000/langchain/ingest \
     -H "Content-Type: application/json" \
     -d '{"document_name": "Test", "text": "Test document text..."}'
   ```

2. **Поиск**:
   ```bash
   curl -X POST http://localhost:8000/langchain/query \
     -H "Content-Type: application/json" \
     -d '{"document_id": "<id>", "query": "test", "top_k": 3}'
   ```

3. **Суммаризация** (требует LLM API key):
   ```bash
   curl -X POST http://localhost:8000/langchain/summarize \
     -H "Content-Type: application/json" \
     -d '{"document_id": "<id>", "strategy": "stuff"}'
   ```

4. **Извлечение пунктов**:
   ```bash
   curl -X POST http://localhost:8000/langchain/extract_points \
     -H "Content-Type: application/json" \
     -d '{"document_id": "<id>", "topics": ["Topic 1"], "summarize": false}'
   ```

---

## 📋 Migration Path (optional)

Если в будущем захотите полностью мигрировать на LangChain:

### Этап 1: Параллельная работа (текущее состояние) ✅
- Оба подхода работают одновременно
- Тестирование LangChain в production
- Сбор метрик качества/производительности

### Этап 2: Постепенная миграция
- Перенаправить часть трафика на LangChain endpoints
- Мониторинг затрат на LLM API
- A/B тестирование результатов

### Этап 3: Полная миграция
- Deprecate оригинальные endpoints
- Удалить дублирующийся код
- Опционально: упростить структуру

**Рекомендация**: Оставить оба подхода, так как они решают разные задачи.

---

## 🐛 Known Limitations

1. **LLM API Latency**: Суммаризация может занять 5-30 секунд в зависимости от размера документа
2. **Token Limits**: OpenAI models имеют лимиты на размер контекста
3. **Russian Language**: Промпты оптимизированы для русского, английский может работать хуже
4. **No Streaming**: Суммаризация возвращает результат только после завершения
5. **No Caching**: Повторные суммаризации одного документа не кэшируются

---

## 🚀 Future Enhancements

Потенциальные улучшения (не реализованы):

1. **Кэширование суммаризаций**
   - Сохранять результаты суммаризации в Qdrant metadata
   - Избегать повторных LLM вызовов

2. **Streaming responses**
   - Возвращать суммаризацию по мере генерации (SSE)

3. **Batch processing**
   - Суммаризация нескольких документов за один запрос

4. **Custom prompts**
   - Позволить пользователям задавать свои промпты

5. **Anthropic support**
   - Полная поддержка Claude API (частично реализовано)

6. **Local LLM support**
   - Интеграция с Ollama для локальных моделей

---

## 📞 Support & Documentation

- **Quick Start**: См. `LANGCHAIN_SETUP.md`
- **Technical Docs**: См. `CLAUDE.md`
- **API Docs**: `http://localhost:8000/docs`
- **LangChain Docs**: https://python.langchain.com/docs/

---

## ✅ Checklist для запуска

- [x] Установлены зависимости (`pip install -r requirements.txt`)
- [x] Создан `.env` файл на основе `.env.example`
- [x] Настроен QDRANT_URL
- [ ] (Опционально) Настроен LLM_API_KEY для суммаризации
- [ ] (Опционально) Установлен LANGCHAIN_ENABLED=true
- [x] Запущен сервер (`uvicorn app.main:app --reload`)
- [x] Проверен http://localhost:8000/docs

---

## 🎓 Credits

- **LangChain Framework**: https://github.com/langchain-ai/langchain
- **Qdrant Vector Database**: https://qdrant.tech/
- **OpenAI API**: https://openai.com/api/
